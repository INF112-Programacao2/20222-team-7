#include "Cliente.h"
#include "Item.h"
#include <string>
#include <algorithm>

class Pedido
{
    protected:
        std::string _nome_cliente();
        double _preco;
        vector <std::string> _lista_itens;
        int _mesa;
        std::string _forma_pagamento;
        int _codigo;

    public:
};